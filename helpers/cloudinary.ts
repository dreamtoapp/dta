// Placeholder helper – kept for API symmetry, returns undefined to allow
// using CSS blur without a fetched dataURL (zero-risk, no network cost).
export async function getBlurDataURL(_publicId: string) {
  return undefined;
}


